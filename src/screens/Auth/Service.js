import React from 'react'
import { FlatList, Animated, Linking, DatePickerIOS, Alert, Platform, TextInput, Keyboard, ActivityIndicator, Text, Dimensions, Modal, ScrollView, ListView, RefreshControl, StyleSheet, TouchableHighlight, TouchableOpacity, TouchableWithoutFeedback, Image, View, VirtualizedList } from 'react-native'

export default class Service extends React.Component {

  render() {
    return (
      <View style={{ flex: 1, backgroundColor: 'blue' }}>
      </View>
    )

  }

}
